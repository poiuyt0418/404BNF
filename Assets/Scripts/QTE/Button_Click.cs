using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Button_Click : QTE_Button
{
    // Start is called before the first frame update
    void Start()
    {

    }

    void OnMouseDown()
    {
        Succeed();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
